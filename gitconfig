[core]
  editor = code --wait

[push]
  default = current

[alias]
  a = add .
  b = branch
  c = checkout
  cb = checkout -b
  d = diff
  p = push
  s = status
  ca = commit --amend
  cm = commit --message
  r = rebase
  ra = rebase --abort
  rc = rebase --continue
  ri = rebase --interactive
