[core]
  editor = code --wait
[alias]
  st = status -sb
  br = branch
  ck = checkout
  ca = commit --amend
  cm = commit --message
  rba = rebase --abort
  rbc = rebase --continue
  rbi = rebase --interactive
  a = add .
  p = push
