[core]
  editor = code --wait

[push]
  default = current

[alias]
  a = add .
  b = branch
  c = checkout
  d = diff
  p = push
  s = status -sb
  ca = commit --amend
  cm = commit --message
  rba = rebase --abort
  rbc = rebase --continue
  rbi = rebase --interactive
